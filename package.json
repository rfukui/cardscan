{
  "name": "mtg-card-scanner-monorepo",
  "private": true,
  "description": "Repository tooling for the MTG Card Scanner monorepo.",
  "scripts": {
    "commitlint": "commitlint --edit",
    "commitlint:from-last": "commitlint --from HEAD~1 --to HEAD",
    "prepare": "sh scripts/install-git-hooks.sh"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0"
  }
}
